{
  "name": "dev-utils",
  "version": "0.0.0",
  "scripts": {
    "build:dev-kit": "npm run build -w=dev-kit",
    "build:dev-feed": "npm run build -w=dev-feed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "packages/dev-kit",
    "packages/dev-feed"
  ],
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.5.4"
  }
}
