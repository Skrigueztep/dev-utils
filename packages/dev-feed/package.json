{
  "name": "dev-feed",
  "version": "0.0.0",
  "bin": {
    "dev-feed": "dist/main.js"
  },
  "scripts": {
    "dev": "tsc --watch",
    "build": "rm -rf dist && tsc",
    "link": "npm run build && npm link --force",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dev-kit": "^0.0.0",
    "playwright": "^1.46.1"
  }
}
