{
  "name": "@skrigueztep/dev-kit",
  "version": "0.0.1-alpha.4",
  "main": "dist/index.js",
  "types": "./dist/types",
  "files": [
    "dist"
  ],
  "bin": {
    "dev-kit": "dist/main.js"
  },
  "repository": {
    "url": "https://github.com/Skrigueztep/dev-utils",
    "type": "git",
    "directory": "packages/dev-kit"
  },
  "scripts": {
    "dev": "tsc --watch",
    "build": "rm -rf dist && tsc && cp -R src/assets dist/assets",
    "link": "npm run build && npm link --force",
    "publish": "npm publish --provenance --access public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "ejs": "^3.1.10"
  },
  "devDependencies": {
    "@types/ejs": "^3.1.5"
  }
}
